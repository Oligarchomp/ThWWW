/// @description Insert description here
// You can write your code in this editor
image_speed = 0;

//image and sprite given by obj_shot_base_dust

rot = rng(360,true,4);

rot_spd = (rng(2,true,4) - 1) * (rng(16,true,2) + 3);
scale = 0.4;
alpha = 0.8;

alpha_acc = 0.05;

angle = 100 - rng(20,true,1);

spd = 2;